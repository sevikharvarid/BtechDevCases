version: '3.9'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=supersecretkeyfor-takehome-only-change-in-prod
    restart: unless-stopped
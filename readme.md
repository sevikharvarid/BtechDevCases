# JWT Auth Take-Home Assignment

Small fullstack auth app with JWT: Go backend + Flutter mobile (

## Branches
- `backend-1` → Go + Fiber JWT backend
- `mobile-1` → Flutter mobile app 

## Backend (Go Fiber + Clean Architecture)

Lokasi: `/backend`

### Cara Run Lokal (Development)
```bash
cd backend
go run cmd/app/main.go